// Legacy index page - now redirected to Dashboard/Auth flow

const Index = () => {
  // This component is no longer used directly as routing is handled in App.tsx
  return null;
};

export default Index;
